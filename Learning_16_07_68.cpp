#include <stdio.h>
int main()

{

/*    {   //Program คำนวนหาจำนวนข้าวใส่กระสอบและหาข้าวที่เหลือ
        float Rice,Bag,Rice_left;

        //
        printf("\n Input Rice :");
        scanf("%f",&Rice);

        Rice_left = (int)Rice  %10;
        Bag = (Rice-Rice_left)/10;

        printf("\n Bag : %.0f\n",Bag);
        printf("\n Rice_Left : %.0f\n",Rice_left);

    }*/


/*    {
        float Bonus,mom,child;  //Data Type & Vairable

        //Input
        printf("\n Dad_Bonus ;");
        scanf("%f",&Bonus);     //Input Dad_Bonus

        //Process
        mom = Bonus*30/100;     //Process Bonus Share To Mom
        child = (Bonus-mom)/3;      //Process Bonus Share To Child

        //Output
        printf("\n Share To Mom : %.2f\n",mom);     //Output Share To Mom
        printf("\n Share To Child : %d\n",3%3);     //Output Share To Child

        return 0;
    }*/


/*    {   //Program คำนวนภาษี
        float salary,Tax,Balance;  //Data Type&Variable

        //Input
        printf("\nSalary :");   //Print Info For User
        scanf("%f",&salary);    //Input Salary

        //Process
        Tax = salary * 3 / 100; //Process Tax
        Balance = salary - Tax; //Process Balance

        //Output
        printf("\nTax :%0.2f\n",Tax);   //Output Tax
        printf("\nBalance :%.2f\n",Balance);    //Output Balance

        return 0; //END

    }*/


/*    {   //Program Calculator Area Triangle
        int base,height;
        float area;

        printf("\nInput Base :");
        scanf("%d",&base);  //Input Base
        printf("\nInput Height :");
        scanf("%d",&height);    //Input Height

        area =(float)1/2*base*height;   //Process 1/2*Base*Height

        printf("\nArea = %.2f\n",area); //Output Area

        return 0;
    }*/


/*    {   //Program คำนวนจำนวนสินค้าและราคาต่อหน่วย



        int num;    //ประกาศตัวแปรจำนวนสินค้า
        float price;    // ประกาศตัวแปรราคาสินค้า
        float Rs;   //ประกาศตัวแปรผลลัพท์

        printf("\nใส่จำนวนสินค้า :");
        scanf("%d",&num);   //รับ Input จำนวนสินค้า
        printf("\nใส่ราคาสินค้าต่อหน่วย :");
        scanf("%f",&price);     //รับ Input ราคาสินค้า

        Rs = num*price;     //Precess จำนวนสินค้า * ราคาสินค้า

        printf("\nราคาสินค้าคือ : %.2f\n บาท",Rs); //Output ราคาสินค้าต่อหน่วย
        return 0;

    }*/
}
